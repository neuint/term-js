import { SizeType } from '../types';
export declare const getItemSize: (container?: HTMLElement, save?: boolean) => SizeType;
export declare const compareItemSize: (first: SizeType, second: SizeType) => boolean;
