{
  "name": "term-js",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "add": "ts-node ./helpers/add-package/index.ts",
    "test": "lerna run test --stream",
    "update:version": "lerna version --no-push"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "terminal-kit": "^1.35.2",
    "ts-node": "^8.8.2"
  },
  "dependencies": {}
}
